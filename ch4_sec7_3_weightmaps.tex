% ======================================================================
%  Section 4.7.3 --- Weight maps.  Replaces the empty heading on p.75.
%  This supersedes the earlier draft, which was built on the occluded
%  structure and weight maps alone and made a weaker claim.
%
%  FIGURES (all four exist):
%    figures/wmap_v3_nom_iter1.png     nominal,  iteration 1
%    figures/wmap_v3_nom_iter600.png   nominal,  iteration 600
%    figures/wmap_v3_occ_iter1.png     occluded, iteration 1
%    figures/wmap_v3_occ_iter600.png   occluded, iteration 600
%
%  MEASURED VALUES, interior region of 66000 measurements, weights in [0,1]:
%
%    map                mean w   w<=0.01   w<0.25   w>=0.99
%    nominal  iter 1     0.768      3.5%    13.6%     24.7%
%    nominal  iter 600   0.789      0.3%     3.2%     14.1%
%    occluded iter 1     0.751      4.2%    16.0%     26.8%
%    occluded iter 600   0.719      9.5%    14.2%     17.8%
%
%    occluder = 4858 connected measurements = 7.4% of 66000
%
%  ALL FOUR PANELS ARE WEIGHT MAPS. The structure maps are NOT used here:
%  their statistics barely change with iteration (nominal 0.306 -> 0.308,
%  occluded 0.300 -> 0.304) because G depends on the image and not on the
%  pose, so one suffices and it belongs in Section 4.4.2 beside eq (4.9),
%  paired with the intensity-based map as the ablation of the measure.
%
%  NOT CLAIMED HERE, DELIBERATELY: that the rejected region is the occluder
%  dilated by the filter support (Section 4.3.2). Measured against the
%  structure map the rejected region is SMALLER, 2367 px against 4858,
%  because the retained boundary rim eats into it and the weights are graded
%  rather than binary. Establishing the dilation needs a measurement against
%  the ellipse in the texture file at a stated threshold.
% ======================================================================

\subsection{Weight maps}
\label{sec:weight-maps}

The pose errors of Sections~\ref{sec:nominal} and~\ref{sec:ablation-results}
establish what the weighting achieves but not how. Since the weights $w_j$ are
defined one per measurement, they can be displayed at their image positions,
and doing so for both scene conditions at the beginning and the end of the
servo makes the mechanism directly observable.
Figure~\ref{fig:wmaps} collects the four maps: white denotes a retained
measurement, $w_j=1$, black a rejected one, $w_j=0$, and the ten-pixel border
that the $9\times9$ filters cannot reach carries no measurement and is shown
mid-grey so as not to be read as rejection. The accompanying statistics are
collected in Table~\ref{tab:weight-stats}.

\paragraph{At the start of the servo the estimator cannot tell the two scenes
apart.}
At iteration~1 the clean and the occluded maps are almost
indistinguishable: the mean weight is $0.768$ against $0.751$, and the
fraction of measurements rejected outright is $3.5\%$ against $4.2\%$. In
neither case does the occluding disc stand out, and in the occluded case the
$4.2\%$ rejected is in fact \emph{less} than the $7.4\%$ of measurements the
disc covers --- the occluder is not yet being rejected at all. The reason is
the robust scale. With the initial misalignment of some $41$~pixels the
residual is large over the whole image, so the median absolute
deviation~\eqref{eq:mad} is large in proportion, and no group of measurements
is extreme relative to it. What rejection does occur is diffuse and spread
across the frame, and since the clean scene contains nothing to reject, all
$13.6\%$ of its strongly down-weighted measurements are valid ones. This is
precisely the difficulty anticipated in Section~\ref{sec:tukey-limitation},
and the nominal map at iteration~1 is its direct measurement.

\paragraph{By convergence the two have separated completely.}
At iteration~600 the clean scene rejects $0.3\%$ of measurements outright and
carries only $3.2\%$ below a quarter weight: the map is white but for a faint
tracery along the strongest edges, where a residual misalignment of a fraction
of a pixel still registers. The weighting has become, to within a third of a
percent, the identity. This is the empirical counterpart of
Remark~\ref{rem:baseline} that the pose errors of Section~\ref{sec:nominal}
could not supply: it is one thing to observe that the weighted variants
converge, and another to see that at convergence they have stopped weighting
anything at all.

The occluded scene at the same iteration rejects $9.5\%$. The disc appears as
a solid, sharply bounded black region, and $9.5\%$ is the $7.4\%$ it covers
together with some two percent of edge response of the same kind as in the
clean map. The rejection is no longer diffuse: it has concentrated onto
exactly the measurements that are corrupted.

\paragraph{The discrimination is a product of convergence, not a precondition
for it.}
Read together, the four panels describe a mechanism that sharpens as the
servo proceeds. While the pose error is large the misalignment residual
dominates, the robust scale is large, and the estimator discriminates poorly
in both scenes alike. As the pose error falls the misalignment residual
collapses, the scale falls with it, and the occluder --- whose residual does
not fall, because no pose can cancel it --- becomes the only large residual in
the image and is rejected decisively. Robust weighting and pose convergence
therefore reinforce one another rather than the weighting simply enabling the
convergence, and the effect is visible in the figure as the divergence of the
right-hand column from the left.

Two consequences follow for how the preceding sections are read. First, the
cost identified in Section~\ref{sec:tukey-limitation} is real but transient:
it is largest when the pose error is largest and has all but vanished by
convergence, falling from $13.6\%$ to $3.2\%$ of measurements in the clean
scene. Second, the failure of Variant~1 under occlusion reported in
Section~\ref{sec:ablation-results} is not attributable to any single
catastrophic iteration but to the accumulation of a bias that the weighted
variants remove progressively, and which they remove most effectively
precisely when the remaining pose error is smallest.

\begin{table}[t]
    \centering
    \caption{Distribution of the Tukey weights of Variant~3 over the
    $66\,000$ measurements of the interior region, for the clean and occluded
    scenes at the first and last iteration. The occluding disc covers $7.4\%$
    of the measurements.}
    \label{tab:weight-stats}
    \small
    \begin{tabular}{@{}llrrrr@{}}
        \toprule
        Scene & Iteration & $\bar{w}$ & $w_j\le0.01$ & $w_j<0.25$ & $w_j\ge0.99$ \\
        \midrule
        clean    & $1$   & $0.768$ & $3.5\%$ & $13.6\%$ & $24.7\%$ \\
        clean    & $600$ & $0.789$ & $0.3\%$ & $\phantom{0}3.2\%$ & $14.1\%$ \\
        occluded & $1$   & $0.751$ & $4.2\%$ & $16.0\%$ & $26.8\%$ \\
        occluded & $600$ & $0.719$ & $9.5\%$ & $14.2\%$ & $17.8\%$ \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{figure}[t]
    \centering
    \begin{subfigure}[b]{0.46\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/wmap_v3_nom_iter1.png}
        \caption{clean scene, iteration~1}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.46\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/wmap_v3_nom_iter600.png}
        \caption{clean scene, iteration~600}
    \end{subfigure}

    \vspace{1em}

    \begin{subfigure}[b]{0.46\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/wmap_v3_occ_iter1.png}
        \caption{occluded scene, iteration~1}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.46\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/wmap_v3_occ_iter600.png}
        \caption{occluded scene, iteration~600}
    \end{subfigure}
    \caption{Tukey weights of Variant~3 displayed at their image positions:
    white retained, black rejected, mid-grey the filter border, which carries
    no measurement. At the first iteration (left column) the two scenes are
    almost indistinguishable and the occluder is not yet isolated, the
    residual being large everywhere and the robust scale large with it. At
    convergence (right column) the clean scene rejects almost nothing while
    the occluded scene rejects the disc alone. The discrimination is therefore
    a product of convergence rather than a precondition for it.}
    \label{fig:wmaps}
\end{figure}

% ======================================================================
%  ELSEWHERE, for consistency with the above:
%
%  1. Section 4.6.4 promises weight maps for Variants 2 and 3. Only
%     Variant 3 produces them. Amend to Variant 3, or add saveMaps() to
%     Variant 2. Variant 1 has D = I and its map is uniformly white, so it
%     should not be promised at all.
%
%  2. Section 4.4.2 should carry ONE structure map, gmap_v3_nom_iter600.png,
%     beside eq (4.9), paired with the intensity-based map gmap_v3_1.png as
%     the ablation of the measure. See the note in ch4_checklist.md step 4.
%
%  3. Section 4.7.1's discussion of what the nominal case establishes can
%     now cite the 0.3% figure above rather than resting on pose errors
%     alone. This is the strongest empirical support for Remark 4.2.2 in the
%     chapter and is worth a forward reference from 4.7.1 to here.
% ======================================================================
