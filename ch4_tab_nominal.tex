% ======================================================================
%  Table for Section 4.7.1 --- nominal case (no occlusion)
%
%  Values marked $^{\dagger}$ are taken from MID-RUN console snapshots
%  (iterations 284 and 601), NOT from the end of the run. Replace each
%  with the last iteration printed before the run stopped.
%
%  The variant assignment of rows 2 and 3 is UNCONFIRMED: the two console
%  captures supplied are numerically identical (see the note below), so
%  which is V2 and which is V3 cannot be established from them.
% ======================================================================

\begin{table}[t]
    \centering
    \caption{Nominal case (unoccluded target), initial pose
    $(0.04,-0.04,1.10)\,\mathrm{m}$, $(12,-12,6)^{\circ}$, desired pose
    $(0,0,1.3)\,\mathrm{m}$, $(0,0,0)^{\circ}$. The absolute stopping
    threshold on $\norm{\mathbf{e}}$ was not reached in any run; convergence
    is therefore reported against the commanded velocity, a run being taken
    as converged once $\norm{\mathbf{V}_c}$ falls below
    $10^{-3}\,\mathrm{m\,s^{-1}}$.}
    \label{tab:ch4-nominal}
    \begin{tabular}{@{}lrrrrr@{}}
        \toprule
        Variant & Iter. & $\norm{\mathbf{e}}_f$
                & $\norm{\Delta\mathbf{t}_f}$ (mm)
                & $\norm{\Delta\boldsymbol{\omega}_f}$ (deg)
                & $\norm{\mathbf{V}_c}_f$ \\
        \midrule
        1 --- none    & 284$^{\dagger}$ & $1.030\times10^{8}$ & 0.561 & 0.332 & $4.80\times10^{-4}$ \\
        2 --- Tukey   & 601$^{\dagger}$ & $1.036\times10^{8}$ & 0.806 & 0.601 & $3.67\times10^{-4}$ \\
        3 --- Hermite & 601$^{\dagger}$ & $1.036\times10^{8}$ & 0.806 & 0.601 & $3.67\times10^{-4}$ \\
        \bottomrule
    \end{tabular}
\end{table}

% ----------------------------------------------------------------------
%  Mean iteration time, if you want it as a separate column or in prose:
%     V1  317.8 ms/iter   (90 249 ms at iteration 284)
%     V2  357.2 ms/iter   (214 653 ms at iteration 601)
%     V3  324.2 ms/iter   (194 831 ms at iteration 601)
%  Note these three are NOT comparable as computational cost, because the
%  runs are at different iteration counts and were not timed under
%  controlled load. If cost is to be reported, time a fixed number of
%  iterations of each variant on an otherwise idle machine.
% ----------------------------------------------------------------------

%  Cross-check worth keeping: the quantity printed as "Ne" is
%  $5.23\log_{10} N_{ei}$ and reads 45.0095 in all three runs, i.e.
%  $N_{ei}=4.04\times10^{8}$ identically. This confirms the three runs
%  began from the same initial image, as Section 4.6.2 requires.
